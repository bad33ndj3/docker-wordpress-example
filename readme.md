## docker wordpress example

### run
    $ docker-compose up -d

### down
    $ docker-compose down

### down with cleanup
    $ docker-compose down --volumes
    
source: `https://docs.docker.com/compose/wordpress/`